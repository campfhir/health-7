{
  "name": "hl7-mapper",
  "version": "1.0.0",
  "description": "TypeScript library for building HL7v2 messages",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "clean": "rm -rf dist",
    "test": "npx tsx --test 'src/**/*.test.ts'",
    "test:watch": "npx tsx --test --watch 'src/**/*.test.ts'"
  },
  "keywords": [
    "hl7",
    "hl7v2",
    "healthcare",
    "message-builder"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0"
  }
}
